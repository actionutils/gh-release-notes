{
  "name": "gh-release-notes",
  "version": "0.0.0",
  "description": "One-shot CLI to draft release notes using release-drafter internals. Node-friendly bundle via ncc.",
  "type": "commonjs",
  "main": "dist/index.cjs",
  "exports": {
    ".": "./dist/index.cjs"
  },
  "bin": {
    "gh-release-notes": "dist/cli.cjs"
  },
  "scripts": {
    "build:lib": "esbuild src/core.ts --bundle --platform=node --format=cjs --target=node18 --external:@actions/core --outfile=dist/index.cjs",
    "build:cli": "esbuild src/cli.ts --bundle --platform=node --format=cjs --target=node18 --external:@actions/core --outfile=dist/cli.cjs",
    "build": "npm run build:lib && npm run build:cli",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node --experimental-vm-modules ./node_modules/jest/bin/jest.js"
  },
  "dependencies": {},
  "overrides": {
    "@actions/core": "file:shims/actions-core"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "@octokit/rest": "^20.1.1",
    "release-drafter": "github:release-drafter/release-drafter#v6.1.0",
    "js-yaml": "^4.1.0",
    "typescript": "^5.4.5",
    "jest": "28.1.3",
    "nock": "^13.3.4"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
