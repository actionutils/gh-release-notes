{
  "name": "@actionutils/gh-release-notes",
  "version": "0.7.0",
  "description": "PR-based release notes generator. Release-drafter compatible CLI using release-drafter internals.",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "bin": {
    "gh-release-notes": "dist/cli.js"
  },
  "scripts": {
    "build:lib": "bun build src/core.ts --target=node --outdir=dist",
    "build:cli": "bun build src/cli.ts --target=node --outdir=dist",
    "build": "bun run build:lib && bun run build:cli",
    "compile": "bun build src/cli.ts --compile --outfile=gh-release-notes",
    "prepublishOnly": "bun run build",
    "test": "bun test",
    "lint": "oxlint --type-aware -D no-explicit-any src",
    "lint:fix": "oxlint --type-aware -D no-explicit-any --fix src",
    "format": "biome format --write src",
    "format:check": "biome format src",
    "check": "bun run lint && bun run format:check && bun run test",
    "dev": "bun run src/cli.ts",
    "clean": "rm -rf dist",
    "typecheck": "bunx tsc --noEmit"
  },
  "overrides": {
    "@actions/core": "file:shims/actions-core"
  },
  "devDependencies": {
    "@actions/core": "file:shims/actions-core",
    "@biomejs/biome": "^2.2.5",
    "@octokit/rest": "^22.0.0",
    "@types/semver": "^7.5.8",
    "@types/bun": "^1.2.23",
    "@types/pino-std-serializers": "^4.0.0",
    "@types/yargs": "^17.0.33",
    "js-yaml": "^4.1.0",
    "minijinja-js": "^2.12.0",
    "oxlint": "^1.19.0",
    "oxlint-tsgolint": "^0.7.1",
    "packageurl-js": "^2.0.1",
    "release-drafter": "github:release-drafter/release-drafter#v6.1.0",
    "semver": "^7.6.3",
    "typescript": "^5.9.3",
    "yargs": "^18.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/actionutils/gh-release-notes"
  }
}
